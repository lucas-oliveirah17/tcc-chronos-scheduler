spring.application.name=backend

# Conexao com o Banco de Dados (Docker)
spring.datasource.url=jdbc:postgresql://localhost:5432/barber_db
spring.datasource.username=barber_admin
spring.datasource.password=tccifsp2025

# Configuracao do Hibernate (JPA)
spring.jpa.hibernate.ddl-auto=update

# Configuracao de Log
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true